name: rocprofiler-systems Build and Test
run-name: build-and-test

# Path exclusions are defined once here — this is the canonical location.
# All four former distro-specific workflow files are replaced by this single
# matrix-driven file.  Adding a new distro means adding one include: entry.

on:
  push:
    branches:
      - develop
    paths:
      - '.github/workflows/rocprofiler-systems-build.yml'
      - 'projects/rocprofiler-systems/**'
      - '!**/*.md'
      - '!**/*.rtf'
      - '!**/*.rst'
      - '!**/.markdownlint-ci2.yaml'
      - '!**/.readthedocs.yaml'
      - '!**/.spellcheck.local.yaml'
      - '!**/.wordlist.txt'
      - '!projects/rocprofiler-systems/docs/**'
      - '!projects/rocprofiler-systems/source/docs/**'
      - '!projects/rocprofiler-systems/source/python/gui/**'
      - '!projects/rocprofiler-systems/docker/**'
      - '!projects/rocprofiler-systems/CMakePresets.json'
  pull_request:
    paths:
      - '.github/workflows/rocprofiler-systems-build.yml'
      - 'projects/rocprofiler-systems/**'
      - '!**/*.md'
      - '!**/*.rtf'
      - '!**/*.rst'
      - '!**/.markdownlint-ci2.yaml'
      - '!**/.readthedocs.yaml'
      - '!**/.spellcheck.local.yaml'
      - '!**/.wordlist.txt'
      - '!projects/rocprofiler-systems/docs/**'
      - '!projects/rocprofiler-systems/source/docs/**'
      - '!projects/rocprofiler-systems/source/python/gui/**'
      - '!projects/rocprofiler-systems/docker/**'
      - '!projects/rocprofiler-systems/CMakePresets.json'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  ROCPROFSYS_CI: ON
  ROCPROFSYS_TMPDIR: "%env{PWD}%/testing-tmp"

jobs:
  # ──────────────────────────────────────────────────────────────────────────
  # Primary builds: all distros × all ROCm versions × g++
  # Replaces: ubuntu-jammy.yml, ubuntu-noble.yml, debian.yml, redhat.yml
  # (primary jobs only — system-deps are in the job below)
  # 26 matrix entries total: 5 jammy + 5 noble + 4 debian + 12 rhel
  #
  # run-ci.py is invoked in four discrete steps:
  #   generate  — writes dashboard_configure/build/test.cmake + CTestCustom.cmake
  #   configure — ctest_start() + cmake configure + CDash submit
  #   build     — ctest_start(APPEND) + cmake build + CDash submit
  #   test      — ctest_start(APPEND) + ctest + CDash submit Done + JUnit
  #
  # YAML anchors:
  #   cmake_flags: anchored on the first entry of each distro group (_cmake-*).
  #   Steps shared with system-deps anchored here (_step-*) and aliased below.
  # ──────────────────────────────────────────────────────────────────────────
  build:
    name: ${{ matrix.name }}
    runs-on: ubuntu-latest
    container:
      image: ${{ matrix.image }}
      options: ${{ matrix.container_opts }}

    strategy:
      fail-fast: false
      matrix:
        include:
          # ── Ubuntu 22.04 (Jammy) ──────────────────────────────────────────
          - name: "Ubuntu 22.04 • g++ • ROCm 6.3"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-6.3-ubuntu-22.04
            distro: jammy
            compiler: g++
            cc: gcc
            cdash_suffix: ubuntu-jammy-g++-rocm-6.3-python-internal-dyninst-hidden-viz
            cmake_flags: &_cmake-jammy >-
              -DCMAKE_INSTALL_PREFIX=/opt/rocprofiler-systems-dev
              -DROCPROFSYS_BUILD_HIDDEN_VISIBILITY=ON
              "-DROCPROFSYS_PYTHON_ENVS=py3.8;py3.9;py3.10;py3.11"
              -DROCPROFSYS_STRIP_LIBRARIES=OFF
              "-DROCPROFSYS_DISABLE_EXAMPLES=transpose;rccl;openmp-target;openmp-vv;videodecode;jpegdecode;network"
              -DUSE_CLANG_OMP=OFF
            python_envs_glob: "py3.{8,9,10,11}"
            install_prefix: /opt/rocprofiler-systems-dev
            install_cleanup: "rm -fr /opt/rocprofiler-systems-dev/"
            cpack_prefix: /opt/rocprofiler-systems
            apt_install_compiler: true
            sanity_check_rocm: true
            test_env_modules: true
            ccache_key_distro: ubuntu-jammy

          - name: "Ubuntu 22.04 • g++ • ROCm 6.4"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-6.4-ubuntu-22.04
            distro: jammy
            compiler: g++
            cc: gcc
            cdash_suffix: ubuntu-jammy-g++-rocm-6.4-python-internal-dyninst-hidden-viz
            cmake_flags: *_cmake-jammy
            python_envs_glob: "py3.{8,9,10,11}"
            install_prefix: /opt/rocprofiler-systems-dev
            install_cleanup: "rm -fr /opt/rocprofiler-systems-dev/"
            cpack_prefix: /opt/rocprofiler-systems
            apt_install_compiler: true
            sanity_check_rocm: true
            test_env_modules: true
            ccache_key_distro: ubuntu-jammy

          - name: "Ubuntu 22.04 • g++ • ROCm 7.0"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.0-ubuntu-22.04
            distro: jammy
            compiler: g++
            cc: gcc
            cdash_suffix: ubuntu-jammy-g++-rocm-7.0-python-internal-dyninst-hidden-viz
            cmake_flags: *_cmake-jammy
            python_envs_glob: "py3.{8,9,10,11}"
            install_prefix: /opt/rocprofiler-systems-dev
            install_cleanup: "rm -fr /opt/rocprofiler-systems-dev/"
            cpack_prefix: /opt/rocprofiler-systems
            apt_install_compiler: true
            sanity_check_rocm: true
            test_env_modules: true
            ccache_key_distro: ubuntu-jammy

          - name: "Ubuntu 22.04 • g++ • ROCm 7.1"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.1-ubuntu-22.04
            distro: jammy
            compiler: g++
            cc: gcc
            cdash_suffix: ubuntu-jammy-g++-rocm-7.1-python-internal-dyninst-hidden-viz
            cmake_flags: *_cmake-jammy
            python_envs_glob: "py3.{8,9,10,11}"
            install_prefix: /opt/rocprofiler-systems-dev
            install_cleanup: "rm -fr /opt/rocprofiler-systems-dev/"
            cpack_prefix: /opt/rocprofiler-systems
            apt_install_compiler: true
            sanity_check_rocm: true
            test_env_modules: true
            ccache_key_distro: ubuntu-jammy

          - name: "Ubuntu 22.04 • g++ • ROCm 7.2"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.2-ubuntu-22.04
            distro: jammy
            compiler: g++
            cc: gcc
            cdash_suffix: ubuntu-jammy-g++-rocm-7.2-python-internal-dyninst-hidden-viz
            cmake_flags: *_cmake-jammy
            python_envs_glob: "py3.{8,9,10,11}"
            install_prefix: /opt/rocprofiler-systems-dev
            install_cleanup: "rm -fr /opt/rocprofiler-systems-dev/"
            cpack_prefix: /opt/rocprofiler-systems
            apt_install_compiler: true
            sanity_check_rocm: true
            test_env_modules: true
            ccache_key_distro: ubuntu-jammy

          # ── Ubuntu 24.04 (Noble) ──────────────────────────────────────────
          - name: "Ubuntu 24.04 • g++ • ROCm 6.3"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-6.3-ubuntu-24.04
            distro: noble
            compiler: g++
            cc: gcc
            cdash_suffix: ubuntu-noble-g++-python-internal-dyninst
            cmake_flags: &_cmake-noble >-
              -DCMAKE_INSTALL_PREFIX=/opt/rocprofiler-systems
              "-DROCPROFSYS_PYTHON_ENVS=py3.8;py3.9;py3.10;py3.11;py3.12;py3.13"
              "-DROCPROFSYS_DISABLE_EXAMPLES=transpose;rccl;openmp-target;openmp-vv-offload;lulesh"
              -DCMAKE_PREFIX_PATH=/opt/rocm
              -DUSE_CLANG_OMP=OFF
            python_envs_glob: "py3.{8,9,10,11,12,13}"
            install_prefix: /opt/rocprofiler-systems
            install_cleanup: "rm -fr /opt/rocprofiler-systems/"
            cpack_prefix: /opt/rocprofiler-systems
            sanity_check_rocm: true
            ccache_key_distro: ubuntu-noble

          - name: "Ubuntu 24.04 • g++ • ROCm 6.4"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-6.4-ubuntu-24.04
            distro: noble
            compiler: g++
            cc: gcc
            cdash_suffix: ubuntu-noble-g++-python-internal-dyninst
            cmake_flags: *_cmake-noble
            python_envs_glob: "py3.{8,9,10,11,12,13}"
            install_prefix: /opt/rocprofiler-systems
            install_cleanup: "rm -fr /opt/rocprofiler-systems/"
            cpack_prefix: /opt/rocprofiler-systems
            sanity_check_rocm: true
            ccache_key_distro: ubuntu-noble

          - name: "Ubuntu 24.04 • g++ • ROCm 7.0"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.0-ubuntu-24.04
            distro: noble
            compiler: g++
            cc: gcc
            cdash_suffix: ubuntu-noble-g++-python-internal-dyninst
            cmake_flags: *_cmake-noble
            python_envs_glob: "py3.{8,9,10,11,12,13}"
            install_prefix: /opt/rocprofiler-systems
            install_cleanup: "rm -fr /opt/rocprofiler-systems/"
            cpack_prefix: /opt/rocprofiler-systems
            sanity_check_rocm: true
            ccache_key_distro: ubuntu-noble

          - name: "Ubuntu 24.04 • g++ • ROCm 7.1"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.1-ubuntu-24.04
            distro: noble
            compiler: g++
            cc: gcc
            cdash_suffix: ubuntu-noble-g++-python-internal-dyninst
            cmake_flags: *_cmake-noble
            python_envs_glob: "py3.{8,9,10,11,12,13}"
            install_prefix: /opt/rocprofiler-systems
            install_cleanup: "rm -fr /opt/rocprofiler-systems/"
            cpack_prefix: /opt/rocprofiler-systems
            sanity_check_rocm: true
            ccache_key_distro: ubuntu-noble

          - name: "Ubuntu 24.04 • g++ • ROCm 7.2"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.2-ubuntu-24.04
            distro: noble
            compiler: g++
            cc: gcc
            cdash_suffix: ubuntu-noble-g++-python-internal-dyninst
            cmake_flags: *_cmake-noble
            python_envs_glob: "py3.{8,9,10,11,12,13}"
            install_prefix: /opt/rocprofiler-systems
            install_cleanup: "rm -fr /opt/rocprofiler-systems/"
            cpack_prefix: /opt/rocprofiler-systems
            sanity_check_rocm: true
            ccache_key_distro: ubuntu-noble

          # ── Debian 12 ────────────────────────────────────────────────────
          - name: "Debian 12 • g++ • ROCm 6.4"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-6.4-debian-12
            distro: debian
            compiler: g++
            cc: gcc
            cdash_suffix: debian-12-g++
            cmake_flags: &_cmake-debian >-
              -DCMAKE_INSTALL_PREFIX=/opt/rocprofiler-systems
              -DROCPROFSYS_USE_OMPT=OFF
              "-DROCPROFSYS_PYTHON_ENVS=py3.8;py3.9;py3.10;py3.11;py3.12;py3.13"
              "-DROCPROFSYS_DISABLE_EXAMPLES=transpose;rccl;openmp-target;openmp-vv-offload;videodecode;jpegdecode;network"
              -DCMAKE_PREFIX_PATH=/opt/rocm
              -DUSE_CLANG_OMP=OFF
            python_envs_glob: "py3.{8,9,10,11,12,13}"
            install_prefix: /opt/rocprofiler-systems
            install_cleanup: "rm -rf /opt/rocprofiler-systems"
            cpack_prefix: /opt/rocprofiler-systems
            ccache_key_distro: debian

          - name: "Debian 12 • g++ • ROCm 7.0"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.0-debian-12
            distro: debian
            compiler: g++
            cc: gcc
            cdash_suffix: debian-12-g++
            cmake_flags: *_cmake-debian
            python_envs_glob: "py3.{8,9,10,11,12,13}"
            install_prefix: /opt/rocprofiler-systems
            install_cleanup: "rm -rf /opt/rocprofiler-systems"
            cpack_prefix: /opt/rocprofiler-systems
            ccache_key_distro: debian

          - name: "Debian 12 • g++ • ROCm 7.1"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.1-debian-12
            distro: debian
            compiler: g++
            cc: gcc
            cdash_suffix: debian-12-g++
            cmake_flags: *_cmake-debian
            python_envs_glob: "py3.{8,9,10,11,12,13}"
            install_prefix: /opt/rocprofiler-systems
            install_cleanup: "rm -rf /opt/rocprofiler-systems"
            cpack_prefix: /opt/rocprofiler-systems
            ccache_key_distro: debian

          - name: "Debian 12 • g++ • ROCm 7.2"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.2-debian-12
            distro: debian
            compiler: g++
            cc: gcc
            cdash_suffix: debian-12-g++
            cmake_flags: *_cmake-debian
            python_envs_glob: "py3.{8,9,10,11,12,13}"
            install_prefix: /opt/rocprofiler-systems
            install_cleanup: "rm -rf /opt/rocprofiler-systems"
            cpack_prefix: /opt/rocprofiler-systems
            ccache_key_distro: debian

          # ── RHEL (all primary entries share the same cmake_flags) ─────────
          # RHEL 8.10 ────────────────────────────────────────────────────────
          - name: "RHEL 8.10 • g++ • ROCm 6.3"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-6.3-rhel-8.10
            distro: rhel-8.10
            compiler: g++
            cc: gcc
            cdash_suffix: rhel-8.10-g++-python-mpip-rocm-6.3
            cmake_flags: &_cmake-rhel-primary >-
              -DCMAKE_INSTALL_PREFIX=/opt/rocprofiler-systems
              -DROCPROFSYS_USE_MPI=OFF
              -DROCPROFSYS_USE_OMPT=OFF
              -DROCPROFSYS_BUILD_BOOST=ON
              -DROCPROFSYS_CI_MPI_RUN_AS_ROOT=ON
              -DROCPROFSYS_INSTALL_PERFETTO_TOOLS=OFF
              "-DROCPROFSYS_PYTHON_ENVS=py3.8;py3.9;py3.10;py3.11"
              "-DROCPROFSYS_DISABLE_EXAMPLES=transpose;rccl;openmp-target;openmp-vv-offload"
            python_envs_glob: "py3.{8,9,10,11}"
            install_prefix: /opt/rocprofiler-systems
            install_cleanup: ""
            cpack_prefix: ""
            container_opts: "--shm-size=512m"
            is_rhel: true
            os_major: "8"
            ccache_key_distro: redhat

          - name: "RHEL 8.10 • g++ • ROCm 6.4"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-6.4-rhel-8.10
            distro: rhel-8.10
            compiler: g++
            cc: gcc
            cdash_suffix: rhel-8.10-g++-python-mpip-rocm-6.4
            cmake_flags: *_cmake-rhel-primary
            python_envs_glob: "py3.{8,9,10,11}"
            install_prefix: /opt/rocprofiler-systems
            install_cleanup: ""
            cpack_prefix: ""
            container_opts: "--shm-size=512m"
            is_rhel: true
            os_major: "8"
            ccache_key_distro: redhat

          - name: "RHEL 8.10 • g++ • ROCm 7.0"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.0-rhel-8.10
            distro: rhel-8.10
            compiler: g++
            cc: gcc
            cdash_suffix: rhel-8.10-g++-python-mpip-rocm-7.0
            cmake_flags: *_cmake-rhel-primary
            python_envs_glob: "py3.{8,9,10,11}"
            install_prefix: /opt/rocprofiler-systems
            install_cleanup: ""
            cpack_prefix: ""
            container_opts: "--shm-size=512m"
            is_rhel: true
            os_major: "8"
            ccache_key_distro: redhat

          - name: "RHEL 8.10 • g++ • ROCm 7.1"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.1-rhel-8.10
            distro: rhel-8.10
            compiler: g++
            cc: gcc
            cdash_suffix: rhel-8.10-g++-python-mpip-rocm-7.1
            cmake_flags: *_cmake-rhel-primary
            python_envs_glob: "py3.{8,9,10,11}"
            install_prefix: /opt/rocprofiler-systems
            install_cleanup: ""
            cpack_prefix: ""
            container_opts: "--shm-size=512m"
            is_rhel: true
            os_major: "8"
            ccache_key_distro: redhat

          - name: "RHEL 8.10 • g++ • ROCm 7.2"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.2-rhel-8.10
            distro: rhel-8.10
            compiler: g++
            cc: gcc
            cdash_suffix: rhel-8.10-g++-python-mpip-rocm-7.2
            cmake_flags: *_cmake-rhel-primary
            python_envs_glob: "py3.{8,9,10,11}"
            install_prefix: /opt/rocprofiler-systems
            install_cleanup: ""
            cpack_prefix: ""
            container_opts: "--shm-size=512m"
            is_rhel: true
            os_major: "8"
            ccache_key_distro: redhat

          # RHEL 9 ───────────────────────────────────────────────────────────
          - name: "RHEL 9 • g++ • ROCm 6.3"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-6.3-rhel-9
            distro: rhel-9
            compiler: g++
            cc: gcc
            cdash_suffix: rhel-9-g++-python-mpip-rocm-6.3
            cmake_flags: *_cmake-rhel-primary
            python_envs_glob: "py3.{8,9,10,11}"
            install_prefix: /opt/rocprofiler-systems
            install_cleanup: ""
            cpack_prefix: ""
            container_opts: "--shm-size=512m"
            is_rhel: true
            os_major: "9"
            ccache_key_distro: redhat

          - name: "RHEL 9 • g++ • ROCm 6.4"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-6.4-rhel-9
            distro: rhel-9
            compiler: g++
            cc: gcc
            cdash_suffix: rhel-9-g++-python-mpip-rocm-6.4
            cmake_flags: *_cmake-rhel-primary
            python_envs_glob: "py3.{8,9,10,11}"
            install_prefix: /opt/rocprofiler-systems
            install_cleanup: ""
            cpack_prefix: ""
            container_opts: "--shm-size=512m"
            is_rhel: true
            os_major: "9"
            ccache_key_distro: redhat

          - name: "RHEL 9 • g++ • ROCm 7.0"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.0-rhel-9
            distro: rhel-9
            compiler: g++
            cc: gcc
            cdash_suffix: rhel-9-g++-python-mpip-rocm-7.0
            cmake_flags: *_cmake-rhel-primary
            python_envs_glob: "py3.{8,9,10,11}"
            install_prefix: /opt/rocprofiler-systems
            install_cleanup: ""
            cpack_prefix: ""
            container_opts: "--shm-size=512m"
            is_rhel: true
            os_major: "9"
            ccache_key_distro: redhat

          - name: "RHEL 9 • g++ • ROCm 7.1"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.1-rhel-9
            distro: rhel-9
            compiler: g++
            cc: gcc
            cdash_suffix: rhel-9-g++-python-mpip-rocm-7.1
            cmake_flags: *_cmake-rhel-primary
            python_envs_glob: "py3.{8,9,10,11}"
            install_prefix: /opt/rocprofiler-systems
            install_cleanup: ""
            cpack_prefix: ""
            container_opts: "--shm-size=512m"
            is_rhel: true
            os_major: "9"
            ccache_key_distro: redhat

          - name: "RHEL 9 • g++ • ROCm 7.2"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.2-rhel-9
            distro: rhel-9
            compiler: g++
            cc: gcc
            cdash_suffix: rhel-9-g++-python-mpip-rocm-7.2
            cmake_flags: *_cmake-rhel-primary
            python_envs_glob: "py3.{8,9,10,11}"
            install_prefix: /opt/rocprofiler-systems
            install_cleanup: ""
            cpack_prefix: ""
            container_opts: "--shm-size=512m"
            is_rhel: true
            os_major: "9"
            ccache_key_distro: redhat

          # RHEL 10 (excludes ROCm 6.3, 6.4, 7.0) ──────────────────────────
          - name: "RHEL 10 • g++ • ROCm 7.1"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.1-rhel-10
            distro: rhel-10
            compiler: g++
            cc: gcc
            cdash_suffix: rhel-10-g++-python-mpip-rocm-7.1
            cmake_flags: *_cmake-rhel-primary
            python_envs_glob: "py3.{8,9,10,11}"
            install_prefix: /opt/rocprofiler-systems
            install_cleanup: ""
            cpack_prefix: ""
            container_opts: "--shm-size=512m"
            is_rhel: true
            os_major: "10"
            ccache_key_distro: redhat

          - name: "RHEL 10 • g++ • ROCm 7.2"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.2-rhel-10
            distro: rhel-10
            compiler: g++
            cc: gcc
            cdash_suffix: rhel-10-g++-python-mpip-rocm-7.2
            cmake_flags: *_cmake-rhel-primary
            python_envs_glob: "py3.{8,9,10,11}"
            install_prefix: /opt/rocprofiler-systems
            install_cleanup: ""
            cpack_prefix: ""
            container_opts: "--shm-size=512m"
            is_rhel: true
            os_major: "10"
            ccache_key_distro: redhat

    env:
      OMPI_ALLOW_RUN_AS_ROOT: 1
      OMPI_ALLOW_RUN_AS_ROOT_CONFIRM: 1
      ROCPROFSYS_CI: 'ON'
      ROCPROFSYS_MAX_THREADS: '64'
      ROCPROFSYS_KEEP_TEST_OUTPUT: '0'
      CCACHE_DIR: ${{ github.workspace }}/.ccache
      GIT_DISCOVERY_ACROSS_FILESYSTEM: 1
      CMAKE_C_COMPILER_LAUNCHER: ccache
      CMAKE_CXX_COMPILER_LAUNCHER: ccache
      # cmake -D flags shared by every primary entry; distro-specific
      # remainder lives in matrix.cmake_flags.
      CMAKE_COMMON_FLAGS: >-
        -DCMAKE_BUILD_TYPE=Release
        -DROCPROFSYS_BUILD_TESTING=ON
        -DROCPROFSYS_USE_PYTHON=ON
        -DROCPROFSYS_BUILD_DYNINST=ON
        -DROCPROFSYS_BUILD_TBB=ON
        -DROCPROFSYS_BUILD_ELFUTILS=ON
        -DROCPROFSYS_BUILD_LIBIBERTY=ON
        -DROCPROFSYS_MAX_THREADS=64
        -DROCPROFSYS_PYTHON_PREFIX=/opt/conda/envs

    steps:
      # Step anchors: steps shared identically with the system-deps job are
      # anchored here (_step-*) and aliased below to avoid duplication.
      - &_step-checkout
        uses: actions/checkout@v6
        with:
          sparse-checkout: |
            projects/rocprofiler-systems/
            .gitmodules

      - name: Configure ROCm environment (Ubuntu / Debian)
        if: ${{ !matrix.is_rhel }}
        shell: bash
        run: |
          echo "/opt/rocm/bin" >> $GITHUB_PATH
          echo "ROCM_PATH=/opt/rocm" >> $GITHUB_ENV
          echo "LD_LIBRARY_PATH=/opt/rocm/lib:${LD_LIBRARY_PATH}" >> $GITHUB_ENV

      - name: Configure RHEL environment
        if: ${{ matrix.is_rhel }}
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          echo "CC=${{ matrix.cc }}" >> $GITHUB_ENV
          echo "CXX=${{ matrix.compiler }}" >> $GITHUB_ENV
          echo "OS_VERSION_MAJOR=$(cat /etc/os-release | grep 'VERSION_ID' | sed 's/=/ /1' | awk '{print $NF}' | sed 's/"//g' | sed 's/\./ /g' | awk '{print $1}')" >> $GITHUB_ENV
          echo "/opt/rocm/bin" >> $GITHUB_PATH
          echo "ROCM_PATH=/opt/rocm" >> $GITHUB_ENV
          echo "LD_LIBRARY_PATH=/opt/rocm/lib:${LD_LIBRARY_PATH}" >> $GITHUB_ENV

      - name: Install compiler packages (Jammy)
        if: ${{ matrix.apt_install_compiler }}
        timeout-minutes: 25
        uses: nick-fields/retry@ad984534de44a9489a53aefd81eb77f87c70dc60 #v4.0.0
        with:
          retry_wait_seconds: 30
          timeout_minutes: 25
          max_attempts: 5
          command: |
            cd projects/rocprofiler-systems/
            apt-get update &&
            apt-get upgrade -y &&
            apt-get install -y ${{ matrix.compiler }} &&
            apt-get autoclean

      - name: Install RHEL-specific packages
        if: ${{ matrix.is_rhel }}
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          if [ "${{ matrix.os_major }}" = "8" ]; then
            wget https://commondatastorage.googleapis.com/perfetto-luci-artifacts/v47.0/linux-amd64/trace_processor_shell -P /opt/trace_processor/bin &&
            chmod +x /opt/trace_processor/bin/trace_processor_shell
            echo "ROCPROFSYS_TRACE_PROC_SHELL=/opt/trace_processor/bin/trace_processor_shell" >> $GITHUB_ENV
          fi
          if [ "${{ matrix.os_major }}" = "10" ]; then
            dnf install -y --enablerepo=crb mpich mpich-devel
            echo "/usr/lib64/mpich/bin" >> $GITHUB_PATH
          fi

      - name: Restore ccache
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/.ccache
          key: ccache-${{ matrix.ccache_key_distro }}-${{ matrix.image }}-${{ github.job }}-${{ matrix.distro }}-${{ matrix.compiler }}-${{ github.sha }}
          restore-keys: |
            ccache-${{ matrix.ccache_key_distro }}-${{ matrix.image }}-${{ github.job }}-${{ matrix.distro }}-${{ matrix.compiler }}-

      - &_step-configure-ccache
        name: Configure ccache
        shell: bash
        run: |
          mkdir -p "${CCACHE_DIR}"
          ccache --max-size=1G
          ccache --set-config=sloppiness=time_macros,include_file_mtime,include_file_ctime,pch_defines
          ccache -z
          ccache -p | head -20

      - &_step-python-deps
        name: Install Python test dependencies
        timeout-minutes: 10
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          for env_dir in /opt/conda/envs/${{ matrix.python_envs_glob }}/; do
            if [ -d "${env_dir}" ] && [ -x "${env_dir}bin/python3" ]; then
              echo "Installing requirements into ${env_dir}"
              "${env_dir}bin/python3" -m pip install -r requirements.txt
            fi
          done

      - &_step-sanity-check-rocm
        name: Sanity check ROCm
        if: ${{ matrix.sanity_check_rocm }}
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          /opt/rocm/bin/hipcc -O3 -c ./examples/transpose/transpose.cpp -o /tmp/transpose.o

      - name: Test Environment Modules
        if: ${{ matrix.test_env_modules }}
        timeout-minutes: 15
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          set -v
          source /usr/share/modules/init/$(basename ${SHELL})
          module avail

      - name: Generate CI scripts
        timeout-minutes: 5
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          git config --global --add safe.directory ${GITHUB_WORKSPACE}
          git config --global --add safe.directory ${PWD}
          echo "CMake: $(cmake --version | head -n 1)"
          echo "Compiler: $(${{ matrix.compiler }} --version | head -n 1)"
          python3 ./scripts/run-ci.py --stage generate \
            --name "${{ github.repository_owner }}-${{ github.ref_name }}-${{ matrix.cdash_suffix }}" \
            --build-jobs $(nproc) \
            --site GitHub \
            -B build \
            -- \
            -DCMAKE_C_COMPILER=${{ matrix.cc }} \
            -DCMAKE_CXX_COMPILER=${{ matrix.compiler }} \
            ${{ env.CMAKE_COMMON_FLAGS }} \
            ${{ matrix.cmake_flags }} \
            -DROCPROFSYS_BUILD_NUMBER=${{ github.run_attempt }} \
            -- \
            -LE "network|gpu"

      - name: Configure
        timeout-minutes: 15
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          python3 ./scripts/run-ci.py --stage configure \
            --name "${{ github.repository_owner }}-${{ github.ref_name }}-${{ matrix.cdash_suffix }}" \
            -B build

      - name: Build
        timeout-minutes: 80
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          python3 ./scripts/run-ci.py --stage build \
            --name "${{ github.repository_owner }}-${{ github.ref_name }}-${{ matrix.cdash_suffix }}" \
            -B build

      - &_step-annotate-build
        name: Annotate build warnings and errors
        if: always()
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          shopt -s nullglob
          logs=(build/Testing/Temporary/LastBuild*.log)
          [ ${#logs[@]} -eq 0 ] && exit 0
          WARNINGS=$(grep -h ' warning:' "${logs[@]}" 2>/dev/null | wc -l || true)
          ERRORS=$(grep -h ' error:' "${logs[@]}" 2>/dev/null | wc -l || true)
          if [ "$((WARNINGS + ERRORS))" -gt 0 ]; then
            echo "::warning::Build: ${ERRORS} error(s), ${WARNINGS} warning(s) — expand step for details"
          fi

      - &_step-cdash-link
        name: CDash link
        if: always()
        shell: bash
        run: |
          NAME="${{ github.repository_owner }}-${{ github.ref_name }}-${{ matrix.cdash_suffix }}"
          # Apply the same PR name transformation as run-ci.py:
          # <owner>-<PR_number>/merge-<suffix> → PR_<PR_number>_<owner>-<suffix>
          NAME=$(echo "${NAME}" | sed 's/\(.*\)-\([0-9]*\)\/merge/PR_\2_\1/')
          URL="https://my.cdash.org/index.php?project=rocprofiler-systems&filtercount=1&showfilters=1&field1=buildname&compare1=65&value1=${NAME}"
          printf '### CDash Results\n[View build on CDash](%s)\n' "${URL}" >> "${GITHUB_STEP_SUMMARY}"
          echo "::notice title=CDash Results::${URL}"

      - name: Test
        timeout-minutes: 60
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          python3 ./scripts/run-ci.py --stage test \
            --name "${{ github.repository_owner }}-${{ github.ref_name }}-${{ matrix.cdash_suffix }}" \
            -B build

      - &_step-upload-junit-results
        name: Upload JUnit test results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: junit-${{ github.job }}-${{ strategy.job-index }}
          path: projects/rocprofiler-systems/build/test-results.xml
          if-no-files-found: ignore

      - &_step-ccache-stats
        name: ccache stats
        if: always()
        shell: bash
        run: |
          echo "=== ccache stats ==="
          ccache -s -v || true
          echo "=== ccache dir size ==="
          du -sh "${CCACHE_DIR}" || true

      - name: Check for leftover buffered files
        timeout-minutes: 5
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          set -v
          if find /tmp -maxdepth 1 -name 'buffered*' -print -quit | grep -q .; then
            echo "Error: Found leftover buffered storage files in /tmp:"
            ls -lh /tmp/buffered*
            exit 1
          else
            echo "✓ No buffered storage files found in /tmp"
          fi

      - name: Install
        timeout-minutes: 10
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          cmake --build build --target install --parallel $(nproc)
          ${{ matrix.install_cleanup }}

      - name: CPack and Install
        if: ${{ !matrix.is_rhel }}
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          cd build
          cpack -G STGZ
          mkdir -p ${{ matrix.cpack_prefix }}
          ./rocprofiler-systems-*.sh --prefix=${{ matrix.cpack_prefix }} --exclude-subdir --skip-license

      - name: Test Install with Modulefile
        if: ${{ !matrix.is_rhel }}
        timeout-minutes: 15
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          set -v
          source /usr/share/modules/init/$(basename ${SHELL})
          module use ${{ matrix.cpack_prefix }}/share/modulefiles
          module avail
          module load rocprofiler-systems
          ./scripts/test-install.sh --test-rocprof-sys-{instrument,avail,sample,python,rewrite,runtime}=1

      - name: Test Install with setup-env.sh
        if: ${{ matrix.is_rhel }}
        timeout-minutes: 10
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          set -v
          source /opt/rocprofiler-systems/share/rocprofiler-systems/setup-env.sh
          export PYTHON_EXECUTABLE=/opt/conda/envs/py3.8/bin/python
          export PATH=/opt/conda/envs/py3.8/bin:${PATH}
          ./scripts/test-install.sh --test-rocprof-sys-{instrument,avail,sample,rewrite,runtime,python}=1

      - name: Test User API
        timeout-minutes: 10
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          set -v
          # For distros that run CPack (non-RHEL), the cmake install prefix is
          # cleaned and CPack re-installs to cpack_prefix.  Use cpack_prefix
          # when set (non-empty), falling back to install_prefix for RHEL which
          # skips CPack and installs directly to install_prefix.
          INSTALL_DIR="${{ matrix.cpack_prefix || matrix.install_prefix }}"
          ./scripts/test-find-package.sh --install-dir "${INSTALL_DIR}"

      - &_step-kill-perfetto
        name: Kill Perfetto
        if: ${{ matrix.is_rhel && (success() || failure()) }}
        continue-on-error: true
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          set +e
          RUNNING_PROCS=$(pgrep trace_processor_shell)
          if [ -n "${RUNNING_PROCS}" ]; then kill -s 9 ${RUNNING_PROCS}; fi

      - &_step-ctest-artifacts
        name: CTest artifacts
        if: failure()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          name: ctest-${{ github.job }}-${{ strategy.job-index }}-log
          path: |
            projects/rocprofiler-systems/build/*.log

      - &_step-data-artifacts
        name: Data artifacts
        if: failure()
        continue-on-error: true
        uses: actions/upload-artifact@v7
        with:
          name: data-${{ github.job }}-${{ strategy.job-index }}-files
          path: |
            projects/rocprofiler-systems/build/rocprofsys-tests-config/*.cfg
            projects/rocprofiler-systems/build/rocprofsys-tests-output/**/*.txt
            projects/rocprofiler-systems/build/rocprofsys-tests-output/**/*-instr*.json

  # ──────────────────────────────────────────────────────────────────────────
  # System-dependency builds: validate that system-provided libs substitute
  # for the bundled ones (BUILD_{TBB,ELFUTILS,LIBIBERTY}=OFF).
  # 8 matrix entries: 2 jammy + 2 noble + 4 rhel (9×2 + 10×2)
  # ──────────────────────────────────────────────────────────────────────────
  system-deps:
    name: ${{ matrix.name }}
    runs-on: ubuntu-latest
    container:
      image: ${{ matrix.image }}
      options: ${{ matrix.container_opts }}

    strategy:
      fail-fast: false
      matrix:
        include:
          # ── Ubuntu 22.04 (Jammy) system-deps ─────────────────────────────
          - name: "Ubuntu 22.04 • g++ • system deps"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.2-ubuntu-22.04
            distro: jammy-system-deps
            compiler: g++
            cc: gcc
            use_clang_omp: "OFF"
            cdash_suffix: ubuntu-jammy-system-deps-g++
            cmake_flags: &_cmake-jammy-sysdeps >-
              -DCMAKE_INSTALL_PREFIX=/opt/rocprofiler-systems
              "-DROCPROFSYS_PYTHON_ENVS=py3.8;py3.9;py3.10;py3.11"
            python_envs_glob: "py3.{8,9,10,11}"
            install_prefix: /opt/rocprofiler-systems
            sanity_check_rocm: true
            ccache_key_distro: ubuntu-jammy-system-deps
            system_deps_apt: >-
              g++ binutils-dev libboost-all-dev libtbb-dev
              libdw-dev libdwarf-dev libelf-dev libiberty-dev

          - name: "Ubuntu 22.04 • amdclang++ • system deps"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.2-ubuntu-22.04
            distro: jammy-system-deps
            compiler: amdclang++
            cc: amdclang
            use_clang_omp: "ON"
            cdash_suffix: ubuntu-jammy-system-deps-amdclang++
            cmake_flags: *_cmake-jammy-sysdeps
            python_envs_glob: "py3.{8,9,10,11}"
            install_prefix: /opt/rocprofiler-systems
            sanity_check_rocm: true
            ccache_key_distro: ubuntu-jammy-system-deps
            system_deps_apt: >-
              g++ binutils-dev libboost-all-dev libtbb-dev
              libdw-dev libdwarf-dev libelf-dev libiberty-dev

          # ── Ubuntu 24.04 (Noble) system-deps ─────────────────────────────
          - name: "Ubuntu 24.04 • g++ • system deps"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.2-ubuntu-24.04
            distro: noble-system-deps
            compiler: g++
            cc: gcc
            use_clang_omp: "OFF"
            cdash_suffix: ubuntu-noble-system-deps-g++
            cmake_flags: &_cmake-noble-sysdeps >-
              -DCMAKE_INSTALL_PREFIX=/opt/rocprofiler-systems-dev
              "-DROCPROFSYS_PYTHON_ENVS=py3.8;py3.9;py3.10;py3.11;py3.12;py3.13"
            python_envs_glob: "py3.{8,9,10,11,12,13}"
            install_prefix: /opt/rocprofiler-systems
            sanity_check_rocm: true
            ccache_key_distro: ubuntu-noble-system-deps
            system_deps_apt: >-
              g++ binutils-dev libboost-all-dev libtbb-dev
              libdw-dev libdwarf-dev libelf-dev libiberty-dev

          - name: "Ubuntu 24.04 • amdclang++ • system deps"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.2-ubuntu-24.04
            distro: noble-system-deps
            compiler: amdclang++
            cc: amdclang
            use_clang_omp: "ON"
            cdash_suffix: ubuntu-noble-system-deps-amdclang++
            cmake_flags: *_cmake-noble-sysdeps
            python_envs_glob: "py3.{8,9,10,11,12,13}"
            install_prefix: /opt/rocprofiler-systems
            sanity_check_rocm: true
            ccache_key_distro: ubuntu-noble-system-deps
            system_deps_apt: >-
              g++ binutils-dev libboost-all-dev libtbb-dev
              libdw-dev libdwarf-dev libelf-dev libiberty-dev

          # ── RHEL 9 system-deps ────────────────────────────────────────────
          - name: "RHEL 9 • g++ • system deps"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.2-rhel-9
            distro: rhel-9-system-deps
            compiler: g++
            cc: gcc
            use_clang_omp: "OFF"
            cdash_suffix: rhel-9-system-deps-g++
            cmake_flags: &_cmake-rhel-sysdeps >-
              -DCMAKE_INSTALL_PREFIX=/opt/rocprofiler-systems-dev
              -DROCPROFSYS_USE_MPI_HEADERS=ON
              -DROCPROFSYS_CI_MPI_RUN_AS_ROOT=ON
              "-DROCPROFSYS_PYTHON_ENVS=py3.8;py3.9;py3.10;py3.11;py3.12;py3.13"
            python_envs_glob: "py3.{8,9,10,11,12,13}"
            install_prefix: /opt/rocprofiler-systems
            container_opts: "--shm-size=512m"
            is_rhel: true
            os_major: "9"
            ccache_key_distro: redhat-system-deps

          - name: "RHEL 9 • amdclang++ • system deps"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.2-rhel-9
            distro: rhel-9-system-deps
            compiler: amdclang++
            cc: amdclang
            use_clang_omp: "ON"
            cdash_suffix: rhel-9-system-deps-amdclang++
            cmake_flags: *_cmake-rhel-sysdeps
            python_envs_glob: "py3.{8,9,10,11,12,13}"
            install_prefix: /opt/rocprofiler-systems
            container_opts: "--shm-size=512m"
            is_rhel: true
            os_major: "9"
            ccache_key_distro: redhat-system-deps

          # ── RHEL 10 system-deps ───────────────────────────────────────────
          - name: "RHEL 10 • g++ • system deps"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.2-rhel-10
            distro: rhel-10-system-deps
            compiler: g++
            cc: gcc
            use_clang_omp: "OFF"
            cdash_suffix: rhel-10-system-deps-g++
            cmake_flags: *_cmake-rhel-sysdeps
            python_envs_glob: "py3.{8,9,10,11,12,13}"
            install_prefix: /opt/rocprofiler-systems
            container_opts: "--shm-size=512m"
            is_rhel: true
            os_major: "10"
            ccache_key_distro: redhat-system-deps

          - name: "RHEL 10 • amdclang++ • system deps"
            image: dgaliffiamd/rocprofiler-systems:ci-rocm-7.2-rhel-10
            distro: rhel-10-system-deps
            compiler: amdclang++
            cc: amdclang
            use_clang_omp: "ON"
            cdash_suffix: rhel-10-system-deps-amdclang++
            cmake_flags: *_cmake-rhel-sysdeps
            python_envs_glob: "py3.{8,9,10,11,12,13}"
            install_prefix: /opt/rocprofiler-systems
            container_opts: "--shm-size=512m"
            is_rhel: true
            os_major: "10"
            ccache_key_distro: redhat-system-deps

    env:
      OMPI_ALLOW_RUN_AS_ROOT: 1
      OMPI_ALLOW_RUN_AS_ROOT_CONFIRM: 1
      ROCPROFSYS_CI: 'ON'
      ROCPROFSYS_MAX_THREADS: '64'
      ROCPROFSYS_KEEP_TEST_OUTPUT: '0'
      CCACHE_DIR: ${{ github.workspace }}/.ccache
      GIT_DISCOVERY_ACROSS_FILESYSTEM: 1
      CMAKE_C_COMPILER_LAUNCHER: ccache
      CMAKE_CXX_COMPILER_LAUNCHER: ccache
      # cmake -D flags shared by every system-deps entry.
      # USE_CLANG_OMP is passed separately from matrix.use_clang_omp so
      # g++ and amdclang++ entries share the same cmake_flags anchor.
      CMAKE_COMMON_FLAGS: >-
        -DCMAKE_BUILD_TYPE=Release
        -DROCPROFSYS_BUILD_TESTING=ON
        -DROCPROFSYS_USE_PYTHON=ON
        -DROCPROFSYS_BUILD_DYNINST=ON
        -DROCPROFSYS_BUILD_TBB=OFF
        -DROCPROFSYS_BUILD_ELFUTILS=OFF
        -DROCPROFSYS_BUILD_LIBIBERTY=OFF
        -DROCPROFSYS_BUILD_HIDDEN_VISIBILITY=ON
        -DROCPROFSYS_STRIP_LIBRARIES=OFF
        -DROCPROFSYS_MAX_THREADS=64
        -DROCPROFSYS_PYTHON_PREFIX=/opt/conda/envs
        "-DROCPROFSYS_DISABLE_EXAMPLES=transpose;rccl;openmp-target;openmp-vv;videodecode;jpegdecode;network"

    steps:
      - *_step-checkout

      - name: Configure ROCm environment (Ubuntu)
        if: ${{ !matrix.is_rhel }}
        shell: bash
        run: |
          echo "/opt/rocm/bin" >> $GITHUB_PATH
          echo "ROCM_PATH=/opt/rocm" >> $GITHUB_ENV
          echo "LD_LIBRARY_PATH=/opt/rocm/lib:${LD_LIBRARY_PATH}" >> $GITHUB_ENV

      - name: Configure RHEL environment
        if: ${{ matrix.is_rhel }}
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          echo "CC=${{ matrix.cc }}" >> $GITHUB_ENV
          echo "CXX=${{ matrix.compiler }}" >> $GITHUB_ENV
          echo "OS_VERSION_MAJOR=$(cat /etc/os-release | grep 'VERSION_ID' | sed 's/=/ /1' | awk '{print $NF}' | sed 's/"//g' | sed 's/\./ /g' | awk '{print $1}')" >> $GITHUB_ENV
          echo "/opt/rocm/bin" >> $GITHUB_PATH
          echo "/opt/rocm/llvm/bin" >> $GITHUB_PATH
          echo "ROCM_PATH=/opt/rocm" >> $GITHUB_ENV
          echo "LD_LIBRARY_PATH=/opt/rocm/lib:${LD_LIBRARY_PATH}" >> $GITHUB_ENV

      - name: Install RHEL mpich (RHEL 10)
        if: ${{ matrix.is_rhel && matrix.os_major == '10' }}
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          dnf install -y --enablerepo=crb mpich mpich-devel
          echo "/usr/lib64/mpich/bin" >> $GITHUB_PATH

      - name: Install system dependency packages (Ubuntu)
        if: ${{ !matrix.is_rhel }}
        timeout-minutes: 25
        uses: nick-fields/retry@ad984534de44a9489a53aefd81eb77f87c70dc60 #v4.0.0
        with:
          retry_wait_seconds: 30
          timeout_minutes: 25
          max_attempts: 5
          command: |
            cd projects/rocprofiler-systems/
            apt-get update &&
            apt-get upgrade -y &&
            apt-get install -y ${{ matrix.system_deps_apt }} &&
            apt-get autoclean

      - name: Install system dependency packages (RHEL)
        if: ${{ matrix.is_rhel }}
        timeout-minutes: 25
        uses: nick-fields/retry@ad984534de44a9489a53aefd81eb77f87c70dc60 #v4.0.0
        with:
          retry_wait_seconds: 30
          timeout_minutes: 25
          max_attempts: 5
          shell: bash
          command: |
            dnf install -y --enablerepo=crb tbb-devel
            dnf install -y binutils-devel
            dnf clean all

      - name: Restore ccache
        uses: actions/cache@v4
        with:
          path: ${{ github.workspace }}/.ccache
          key: ccache-${{ matrix.ccache_key_distro }}-${{ matrix.image }}-${{ github.job }}-${{ matrix.compiler }}-${{ github.sha }}
          restore-keys: |
            ccache-${{ matrix.ccache_key_distro }}-${{ matrix.image }}-${{ github.job }}-${{ matrix.compiler }}-

      - *_step-configure-ccache

      - *_step-python-deps

      - *_step-sanity-check-rocm

      - name: Generate CI scripts
        timeout-minutes: 5
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          git config --global --add safe.directory ${GITHUB_WORKSPACE}
          git config --global --add safe.directory ${PWD}
          echo "CMake: $(cmake --version | head -n 1)"
          echo "Compiler: $(${{ matrix.compiler }} --version | head -n 1)"
          echo "Testing build with SYSTEM dependencies (ROCPROFSYS_BUILD_{TBB,ELFUTILS,LIBIBERTY}=OFF)"
          python3 ./scripts/run-ci.py --stage generate \
            --name "${{ github.repository_owner }}-${{ github.ref_name }}-${{ matrix.cdash_suffix }}" \
            --build-jobs $(nproc) \
            --site GitHub \
            -B build \
            -- \
            -DCMAKE_C_COMPILER=${{ matrix.cc }} \
            -DCMAKE_CXX_COMPILER=${{ matrix.compiler }} \
            ${{ env.CMAKE_COMMON_FLAGS }} \
            ${{ matrix.cmake_flags }} \
            -DUSE_CLANG_OMP=${{ matrix.use_clang_omp }} \
            -DROCPROFSYS_BUILD_NUMBER=${{ github.run_attempt }} \
            -- \
            -LE "network|gpu"

      - name: Configure
        timeout-minutes: 15
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          python3 ./scripts/run-ci.py --stage configure \
            --name "${{ github.repository_owner }}-${{ github.ref_name }}-${{ matrix.cdash_suffix }}" \
            -B build

      - name: Build
        timeout-minutes: 80
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          python3 ./scripts/run-ci.py --stage build \
            --name "${{ github.repository_owner }}-${{ github.ref_name }}-${{ matrix.cdash_suffix }}" \
            -B build

      - *_step-annotate-build

      - *_step-cdash-link

      - name: Test
        timeout-minutes: 60
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          python3 ./scripts/run-ci.py --stage test \
            --name "${{ github.repository_owner }}-${{ github.ref_name }}-${{ matrix.cdash_suffix }}" \
            -B build

      - *_step-upload-junit-results

      - *_step-ccache-stats

      - name: Install
        timeout-minutes: 10
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          cmake --install build --prefix ${{ matrix.install_prefix }}

      - name: Test Install with Modulefile
        if: ${{ !matrix.is_rhel }}
        timeout-minutes: 15
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          set -v
          source /usr/share/modules/init/$(basename ${SHELL})
          module use ${{ matrix.install_prefix }}/share/modulefiles
          module avail
          module load rocprofiler-systems
          ./scripts/test-install.sh --test-rocprof-sys-{instrument,avail,sample,python,rewrite,runtime}=1

      - name: Test Install with setup-env.sh
        if: ${{ matrix.is_rhel }}
        timeout-minutes: 10
        shell: bash
        working-directory: projects/rocprofiler-systems/
        run: |
          set -v
          source ${{ matrix.install_prefix }}/share/rocprofiler-systems/setup-env.sh
          export PYTHON_EXECUTABLE=/opt/conda/envs/py3.8/bin/python
          export PATH=/opt/conda/envs/py3.8/bin:${PATH}
          ./scripts/test-install.sh --test-rocprof-sys-{instrument,avail,sample,rewrite,runtime,python}=1

      - *_step-kill-perfetto

      - *_step-ctest-artifacts

      - *_step-data-artifacts

  publish-test-results:
    name: Publish test result summary
    needs: [build, system-deps]
    if: always()
    runs-on: ubuntu-latest
    permissions:
      actions: read
      checks: write
      contents: read
      issues: write
      pull-requests: write
    steps:
      - name: Download JUnit test result artifacts
        uses: actions/download-artifact@v7
        continue-on-error: true
        with:
          pattern: junit-*
          path: junit-results
          merge-multiple: false

      - name: Check for JUnit files
        id: junit-files
        shell: bash
        run: |
          shopt -s globstar nullglob
          files=(junit-results/**/*.xml)
          echo "count=${#files[@]}" >> "${GITHUB_OUTPUT}"

      - name: Publish test result summary
        if: steps.junit-files.outputs.count != '0'
        uses: EnricoMi/publish-unit-test-result-action@v2
        with:
          files: junit-results/**/*.xml
          check_name: rocprofiler-systems Tests
          comment_mode: ${{ github.event_name == 'pull_request' && 'always' || 'off' }}
          comment_title: rocprofiler-systems Test Summary
          check_run: true
          job_summary: true
          report_individual_runs: false
          fail_on: nothing
          action_fail: false

